import type { JsonFormatterMode } from '@/features/tools/json-formatter/model/types'

export const JSON_FORMAT_INDENT = 2

export const DEFAULT_JSON_FORMATTER_MODE: JsonFormatterMode = 'format'

export const DEFAULT_JSON_FORMATTER_INPUT = ''
